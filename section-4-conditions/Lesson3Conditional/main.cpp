#include <iostream>

using namespace std;

int main()
{
    int a = 30;
    int b = 40;

    //CONDITION
    string message = (a > b) ? "a > b" : "a <= b";

    cout << message << endl;

    cout << ((7 < 4) ? 7 : 4) << endl;
}
